apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: python-web-app
  name: python-web-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-web-app
  template:
    metadata:
      labels:
        app: python-web-app
    spec:
      containers:
      - image: navabnaeem/python-images:version-print
        name: nginx
        command: 
        - /bin/sh
        - -c
        - exit 1
        env:
        - name: APP_VERSION
          value: v4
